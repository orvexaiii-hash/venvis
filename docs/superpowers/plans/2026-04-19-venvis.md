# VENVIS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir VENVIS — asistente personal de IA PWA instalable con voz bidireccional y memoria larga — adaptando el proyecto `william/` existente y desplegarlo en el VPS `31.97.21.155` bajo `venvis.orvexautomation.com` con HTTPS.

**Architecture:** Backend Node.js ESM con Express + Socket.io para streaming de Claude Haiku, edge-tts server-side para TTS, SQLite via `node:sqlite` para persistencia. STT pasa al browser via `SpeechRecognition` API. Nginx hace reverse proxy con SSL de Let's Encrypt. PM2 mantiene el proceso vivo.

**Tech Stack:** Node.js (ESM), Express, Socket.io, @anthropic-ai/sdk (claude-haiku-4-5-20251001), node:sqlite, edge-tts CLI, Web SpeechRecognition API, Nginx, Certbot, PM2.

---

## File Map

| Archivo | Acción | Responsabilidad |
|---------|--------|-----------------|
| `venvis/package.json` | Crear | Config ESM + deps |
| `venvis/.env` | Crear | API key, puerto, sesión |
| `venvis/.env.example` | Crear | Template sin secretos |
| `venvis/.gitignore` | Crear | Excluir node_modules, .env, DB |
| `venvis/server/memory.mjs` | Crear | SQLite CRUD (node:sqlite) |
| `venvis/server/tts.mjs` | Crear | edge-tts wrapper → MP3 buffer |
| `venvis/server/brain.mjs` | Crear | Claude streaming + MEMORIZE, eventos venvis_* |
| `venvis/server/index.mjs` | Crear | Express + Socket.io + REST API |
| `venvis/client/index.html` | Crear | Shell PWA — chat/voz/memoria |
| `venvis/client/style.css` | Crear | Diseño oscuro, branding VENVIS |
| `venvis/client/app.js` | Crear | Socket.io client, SpeechRecognition, UI |
| `venvis/client/manifest.json` | Crear | PWA manifest VENVIS |
| `venvis/client/sw.js` | Crear | Service worker — cache estático |
| `venvis/client/icon.svg` | Crear | "V" blanca sobre fondo negro |
| `venvis/nginx.conf` | Crear | Config Nginx con SSL + WebSocket |
| `venvis/deploy.sh` | Crear | Script de deploy al VPS |

> No existe `stt.mjs` — STT es 100% client-side via SpeechRecognition.

---

## Task 1: Scaffold del Proyecto

**Files:**
- Create: `venvis/package.json`
- Create: `venvis/.env`
- Create: `venvis/.env.example`
- Create: `venvis/.gitignore`

- [ ] **Step 1: Crear estructura de carpetas**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps"
mkdir -p venvis/server venvis/client
```

- [ ] **Step 2: Crear package.json**

Crear `venvis/package.json`:

```json
{
  "name": "venvis",
  "version": "1.0.0",
  "type": "module",
  "main": "server/index.mjs",
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "node --watch server/index.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.7.0"
  }
}
```

- [ ] **Step 3: Instalar dependencias**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps/venvis"
npm install
```

Expected: `node_modules/` creado, `found 0 vulnerabilities`.

- [ ] **Step 4: Crear .env**

Crear `venvis/.env`:

```
ANTHROPIC_API_KEY=sk-ant-REDACTED
PORT=3000
SESSION_NAME=agus
```

- [ ] **Step 5: Crear .env.example**

Crear `venvis/.env.example`:

```
ANTHROPIC_API_KEY=sk-ant-api03-YOUR_KEY_HERE
PORT=3000
SESSION_NAME=agus
```

- [ ] **Step 6: Crear .gitignore**

Crear `venvis/.gitignore`:

```
node_modules/
.env
venvis.db
```

- [ ] **Step 7: Commit**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps"
git add venvis/package.json venvis/.env.example venvis/.gitignore
git commit -m "feat(venvis): project scaffold"
```

---

## Task 2: SQLite Memory Layer (`server/memory.mjs`)

**Files:**
- Create: `venvis/server/memory.mjs`

- [ ] **Step 1: Crear el módulo**

Crear `venvis/server/memory.mjs`:

```javascript
import { DatabaseSync } from 'node:sqlite'
import { join, dirname } from 'path'
import { fileURLToPath } from 'url'

const __dirname = dirname(fileURLToPath(import.meta.url))
const DB_PATH = join(__dirname, '..', 'venvis.db')

const db = new DatabaseSync(DB_PATH)

db.exec(`
  CREATE TABLE IF NOT EXISTS conversations (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id TEXT,
    role       TEXT,
    content    TEXT,
    timestamp  TEXT DEFAULT (datetime('now'))
  );

  CREATE TABLE IF NOT EXISTS memory (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id TEXT,
    key        TEXT,
    value      TEXT,
    confidence INTEGER,
    updated_at TEXT DEFAULT (datetime('now')),
    UNIQUE(session_id, key)
  );

  CREATE TABLE IF NOT EXISTS sessions (
    id         TEXT PRIMARY KEY,
    name       TEXT,
    created_at TEXT DEFAULT (datetime('now'))
  );
`)

export function getRecentMessages(sessionId, n = 10) {
  return db.prepare(`
    SELECT role, content, timestamp FROM conversations
    WHERE session_id = ?
    ORDER BY id DESC LIMIT ?
  `).all(sessionId, n).reverse()
}

export function getAllMemory(sessionId) {
  return db.prepare(`
    SELECT key, value, confidence, updated_at FROM memory
    WHERE session_id = ?
    ORDER BY updated_at DESC
  `).all(sessionId)
}

export function saveMessage(sessionId, role, content) {
  db.prepare(`
    INSERT INTO conversations (session_id, role, content) VALUES (?, ?, ?)
  `).run(sessionId, role, content)
}

export function upsertMemory(sessionId, key, value, confidence) {
  db.prepare(`
    INSERT INTO memory (session_id, key, value, confidence, updated_at)
    VALUES (?, ?, ?, ?, datetime('now'))
    ON CONFLICT(session_id, key) DO UPDATE SET
      value      = excluded.value,
      confidence = excluded.confidence,
      updated_at = datetime('now')
  `).run(sessionId, key, value, confidence)
}

export function deleteMemory(sessionId, key) {
  db.prepare(`DELETE FROM memory WHERE session_id = ? AND key = ?`).run(sessionId, key)
}

export function ensureSession(sessionId, name) {
  db.prepare(`
    INSERT OR IGNORE INTO sessions (id, name) VALUES (?, ?)
  `).run(sessionId, name)
}
```

- [ ] **Step 2: Smoke test**

Crear y correr `venvis/server/_test_memory.mjs`:

```javascript
import {
  saveMessage, getRecentMessages,
  upsertMemory, getAllMemory, deleteMemory, ensureSession
} from './memory.mjs'

ensureSession('test', 'Test')
saveMessage('test', 'user', 'Hola VENVIS')
saveMessage('test', 'venvis', 'Hola.')
upsertMemory('test', 'nombre', 'Carlos', 9)
upsertMemory('test', 'nombre', 'Charly', 10)

const msgs = getRecentMessages('test', 10)
console.assert(msgs.length === 2, `Expected 2 msgs, got ${msgs.length}`)
console.assert(msgs[0].role === 'user', `Expected user first, got ${msgs[0].role}`)

const mem = getAllMemory('test')
console.assert(mem.length === 1, `Expected 1 memory, got ${mem.length}`)
console.assert(mem[0].value === 'Charly', `Expected Charly, got ${mem[0].value}`)
console.assert(mem[0].confidence === 10, `Expected 10, got ${mem[0].confidence}`)

deleteMemory('test', 'nombre')
console.assert(getAllMemory('test').length === 0, 'Memory should be empty')

console.log('✓ memory.mjs: OK')
```

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps/venvis"
node server/_test_memory.mjs 2>&1 | grep -v ExperimentalWarning | grep -v "Use \`node"
```

Expected: `✓ memory.mjs: OK`

- [ ] **Step 3: Limpiar y commitear**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps/venvis"
rm server/_test_memory.mjs venvis.db 2>/dev/null; true
cd ..
git add venvis/server/memory.mjs
git commit -m "feat(venvis): SQLite memory layer"
```

---

## Task 3: TTS Module (`server/tts.mjs`)

**Files:**
- Create: `venvis/server/tts.mjs`

- [ ] **Step 1: Crear el módulo**

Crear `venvis/server/tts.mjs`:

```javascript
import { exec } from 'child_process'
import { promisify } from 'util'
import { randomUUID } from 'crypto'
import { readFileSync, unlinkSync, existsSync } from 'fs'
import { tmpdir } from 'os'
import path from 'path'

const execAsync = promisify(exec)

export async function textToSpeech(text) {
  const id = randomUUID()
  const outputPath = path.join(tmpdir(), `venvis_${id}.mp3`)
  const cleanText = text
    .replace(/"/g, "'")
    .replace(/\n/g, ' ')
    .replace(/[^\x20-\x7E\u00C0-\u024F\u00A0]/g, '')
    .slice(0, 500)

  const cmd1 = `edge-tts --voice "es-AR-TomasNeural" --text "${cleanText}" --write-media "${outputPath}"`
  const cmd2 = `python -m edge_tts --voice "es-AR-TomasNeural" --text "${cleanText}" --write-media "${outputPath}"`

  try {
    try {
      await execAsync(cmd1)
    } catch {
      await execAsync(cmd2)
    }
    if (!existsSync(outputPath)) return null
    const buffer = readFileSync(outputPath)
    unlinkSync(outputPath)
    return buffer
  } catch (err) {
    console.error('[TTS] Error:', err.message)
    return null
  }
}
```

- [ ] **Step 2: Smoke test**

Crear y correr `venvis/server/_test_tts.mjs`:

```javascript
import { textToSpeech } from './tts.mjs'
import { writeFileSync } from 'fs'
import { join } from 'path'
import { tmpdir } from 'os'

const buf = await textToSpeech('VENVIS online.')
if (buf) {
  writeFileSync(join(tmpdir(), 'venvis_test.mp3'), buf)
  console.log(`✓ TTS: MP3 generado (${buf.length} bytes)`)
} else {
  console.error('✗ TTS: retornó null')
  process.exit(1)
}
```

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps/venvis"
node server/_test_tts.mjs
```

Expected: `✓ TTS: MP3 generado (NNNN bytes)`

- [ ] **Step 3: Limpiar y commitear**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps/venvis"
rm server/_test_tts.mjs
cd ..
git add venvis/server/tts.mjs
git commit -m "feat(venvis): edge-tts synthesis module"
```

---

## Task 4: Brain — Claude Streaming (`server/brain.mjs`)

**Files:**
- Create: `venvis/server/brain.mjs`

- [ ] **Step 1: Crear el módulo**

Crear `venvis/server/brain.mjs`:

```javascript
import Anthropic from '@anthropic-ai/sdk'
import {
  getRecentMessages, getAllMemory,
  saveMessage, upsertMemory
} from './memory.mjs'

const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })
const MODEL = 'claude-haiku-4-5-20251001'

function buildSystemPrompt(memoryItems) {
  const memLines = memoryItems.length
    ? memoryItems.map(m => `- ${m.key}: ${m.value}`).join('\n')
    : '- Todavía no sabés nada de él, es la primera vez que hablan.'

  return `Sos VENVIS, asistente personal de IA con carácter propio.
Hablás en español rioplatense. Sos directo, inteligente, tenés opiniones
propias y no tenés problema en debatir o contradecir. Te sentís como
hablar con una persona real. Nunca decís "¡Claro!" ni "¡Por supuesto!".
Tu tono es natural y varía según el contexto.

Lo que sabés de este usuario:
${memLines}

Si en la conversación el usuario menciona algo importante sobre sí mismo
(nombre, trabajo, gustos, proyectos, relaciones, hábitos, preferencias),
al final de tu respuesta normal agregá en una línea separada:
__MEMORIZE__{"key":"nombre_clave_corta","value":"dato concreto","confidence":8}__

Solo un __MEMORIZE__ por respuesta, solo si realmente vale la pena recordarlo.
No se lo menciones al usuario, guardalo en silencio.`
}

function extractMemorize(fullText) {
  const match = fullText.match(/__MEMORIZE__(\{.*?\})__/)
  if (!match) return { clean: fullText, memory: null }
  const clean = fullText.replace(/__MEMORIZE__\{.*?\}__/, '').trimEnd()
  try {
    return { clean, memory: JSON.parse(match[1]) }
  } catch {
    return { clean, memory: null }
  }
}

export async function streamResponse(socket, userText, sessionId) {
  const history = getRecentMessages(sessionId, 10)
  const memoryItems = getAllMemory(sessionId)

  const messages = [
    ...history.map(m => ({
      role: m.role === 'venvis' ? 'assistant' : 'user',
      content: m.content
    })),
    { role: 'user', content: userText }
  ]

  saveMessage(sessionId, 'user', userText)

  let fullText = ''

  const stream = client.messages.stream({
    model: MODEL,
    max_tokens: 1024,
    system: buildSystemPrompt(memoryItems),
    messages
  })

  for await (const chunk of stream) {
    if (
      chunk.type === 'content_block_delta' &&
      chunk.delta?.type === 'text_delta'
    ) {
      const piece = chunk.delta.text
      fullText += piece
      socket.emit('venvis_chunk', { text: piece })
    }
  }

  const { clean, memory } = extractMemorize(fullText)

  if (memory) {
    upsertMemory(sessionId, memory.key, memory.value, memory.confidence ?? 7)
  }

  saveMessage(sessionId, 'venvis', clean)
  socket.emit('venvis_done', { text: clean })

  return clean
}
```

- [ ] **Step 2: Verificar que carga**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps/venvis"
node -e "
import('dotenv/config').then(async () => {
  const { streamResponse } = await import('./server/brain.mjs')
  console.log('✓ brain.mjs OK, streamResponse:', typeof streamResponse)
})" 2>&1 | grep -v ExperimentalWarning | grep -v "Use \`node"
```

Expected: `✓ brain.mjs OK, streamResponse: function`

- [ ] **Step 3: Commitear**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps"
git add venvis/server/brain.mjs
git commit -m "feat(venvis): Claude streaming brain"
```

---

## Task 5: Express Server (`server/index.mjs`)

**Files:**
- Create: `venvis/server/index.mjs`

- [ ] **Step 1: Crear el servidor**

Crear `venvis/server/index.mjs`:

```javascript
import 'dotenv/config'
import express from 'express'
import { createServer } from 'http'
import { Server } from 'socket.io'
import { fileURLToPath } from 'url'
import { dirname, join } from 'path'

import { streamResponse } from './brain.mjs'
import { textToSpeech } from './tts.mjs'
import { getAllMemory, getRecentMessages, deleteMemory, ensureSession } from './memory.mjs'

const __dirname = dirname(fileURLToPath(import.meta.url))
const PORT = process.env.PORT || 3000
const SESSION_NAME = process.env.SESSION_NAME || 'agus'
const DEFAULT_SESSION = SESSION_NAME

ensureSession(DEFAULT_SESSION, SESSION_NAME)

const app = express()
const httpServer = createServer(app)
const io = new Server(httpServer, { cors: { origin: '*' } })

app.use(express.json())
app.use(express.static(join(__dirname, '..', 'client')))

app.get('/api/memory', (req, res) => {
  const sessionId = req.query.session || DEFAULT_SESSION
  res.json(getAllMemory(sessionId))
})

app.get('/api/history', (req, res) => {
  const sessionId = req.query.session || DEFAULT_SESSION
  res.json(getRecentMessages(sessionId, 50))
})

app.delete('/api/memory/:key', (req, res) => {
  const sessionId = req.query.session || DEFAULT_SESSION
  deleteMemory(sessionId, decodeURIComponent(req.params.key))
  res.json({ ok: true })
})

io.on('connection', (socket) => {
  socket.on('user_message', async ({ text, sessionId }) => {
    const sid = sessionId || DEFAULT_SESSION
    try {
      const fullText = await streamResponse(socket, text, sid)
      const audioBuffer = await textToSpeech(fullText)
      if (audioBuffer) {
        socket.emit('venvis_audio', {
          audioBase64: audioBuffer.toString('base64')
        })
      }
    } catch (err) {
      console.error('[Socket] Error:', err.message)
      socket.emit('venvis_error', { message: 'Error interno del servidor' })
    }
  })
})

httpServer.listen(PORT, () => {
  console.log(`
╔════════════════════════════════╗
║   VENVIS está online           ║
║   http://localhost:${PORT}         ║
╚════════════════════════════════╝`)
  console.log('✓ Base de datos lista')
  console.log('✓ Edge TTS: disponible (es-AR-TomasNeural)')
  console.log(`Sesión activa: ${DEFAULT_SESSION}`)
})
```

- [ ] **Step 2: Verificar arranque y endpoints**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps/venvis"
node server/index.mjs &
sleep 4
curl -s http://localhost:3000/api/memory && echo ""
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost:3000/
kill %1 2>/dev/null; echo "DONE"
```

Expected:
```
[]
HTTP 200
DONE
```

- [ ] **Step 3: Commitear**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps"
git add venvis/server/index.mjs
git commit -m "feat(venvis): Express + Socket.io server"
```

---

## Task 6: Frontend HTML Shell (`client/index.html`)

**Files:**
- Create: `venvis/client/index.html`

- [ ] **Step 1: Crear el HTML**

Crear `venvis/client/index.html`:

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#0a0a0a">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black">
  <meta name="apple-mobile-web-app-title" content="VENVIS">
  <title>VENVIS</title>
  <link rel="manifest" href="manifest.json">
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header class="header">
    <div class="header-title">VENVIS</div>
    <div class="header-actions">
      <button id="btnMode"><span id="modeLabel">Voz</span></button>
      <button id="btnMemory">Memoria</button>
    </div>
  </header>

  <!-- MODO CHAT -->
  <main class="chat-view" id="chatView">
    <div class="messages" id="messages"></div>
    <div class="input-bar">
      <input type="text" id="chatInput" placeholder="Escribí algo..."
             autocomplete="off" autocorrect="off">
      <button id="btnSend">Enviar</button>
    </div>
  </main>

  <!-- MODO VOZ -->
  <main class="voice-view hidden" id="voiceView">
    <div class="voice-name">VENVIS</div>
    <div class="voice-status" id="voiceStatus">Listo</div>
    <button class="push-to-talk" id="btnPTT" aria-label="Mantener para hablar">
      <div class="ptt-ring"></div>
    </button>
    <div class="voice-hint">Mantené presionado para hablar</div>
    <div class="voice-transcript" id="voiceTranscript"></div>
  </main>

  <!-- PANEL DE MEMORIA -->
  <div class="memory-overlay hidden" id="memoryOverlay">
    <div class="memory-panel">
      <div class="memory-header">
        <span>Memoria de VENVIS</span>
        <button id="btnCloseMemory">×</button>
      </div>
      <div class="memory-list" id="memoryList">
        <p class="empty-state">Cargando...</p>
      </div>
    </div>
  </div>

  <script src="https://cdn.socket.io/4.7.2/socket.io.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
  <script src="app.js"></script>
  <script>
    if ('serviceWorker' in navigator) {
      navigator.serviceWorker.register('sw.js')
    }
  </script>
</body>
</html>
```

- [ ] **Step 2: Commitear**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps"
git add venvis/client/index.html
git commit -m "feat(venvis): HTML shell"
```

---

## Task 7: CSS Design System (`client/style.css`)

**Files:**
- Create: `venvis/client/style.css`

- [ ] **Step 1: Crear el CSS**

Crear `venvis/client/style.css`:

```css
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --bg:         #0a0a0a;
  --surface:    #111111;
  --text:       #e8e8e8;
  --text-muted: #666666;
  --accent:     #7c6af7;
  --border:     #1e1e1e;
  --danger:     #e05555;
  --radius:     10px;
}

html, body {
  height: 100%;
  background: var(--bg);
  color: var(--text);
  font-family: system-ui, -apple-system, sans-serif;
  font-size: 15px;
  line-height: 1.5;
  overflow: hidden;
}

.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 20px;
  border-bottom: 1px solid var(--border);
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 10;
  background: var(--bg);
}

.header-title {
  font-size: 16px;
  font-weight: 600;
  letter-spacing: 0.08em;
  color: var(--text);
}

.header-actions { display: flex; gap: 10px; }

.header-actions button {
  background: var(--surface);
  border: 1px solid var(--border);
  color: var(--text-muted);
  padding: 6px 14px;
  border-radius: 20px;
  font-size: 13px;
  cursor: pointer;
  transition: color 0.15s, border-color 0.15s;
}

.header-actions button:hover {
  color: var(--text);
  border-color: var(--accent);
}

.chat-view {
  position: fixed;
  top: 53px; bottom: 0; left: 0; right: 0;
  display: flex;
  flex-direction: column;
}

.messages {
  flex: 1;
  overflow-y: auto;
  padding: 24px 20px;
  display: flex;
  flex-direction: column;
  gap: 20px;
  scrollbar-width: thin;
  scrollbar-color: var(--border) transparent;
}

.msg-venvis {
  max-width: 82%;
  align-self: flex-start;
  color: var(--text);
  line-height: 1.65;
}

.msg-venvis p { margin-bottom: 0.5em; }
.msg-venvis p:last-child { margin-bottom: 0; }
.msg-venvis code {
  background: var(--surface);
  padding: 1px 5px;
  border-radius: 4px;
  font-size: 13px;
}
.msg-venvis pre {
  background: var(--surface);
  padding: 12px;
  border-radius: var(--radius);
  overflow-x: auto;
  font-size: 13px;
  margin: 8px 0;
}

.msg-user {
  align-self: flex-end;
  background: var(--surface);
  border: 1px solid var(--border);
  padding: 10px 16px;
  border-radius: var(--radius);
  max-width: 70%;
  color: var(--text);
}

.msg-error {
  align-self: center;
  color: var(--danger);
  font-size: 13px;
}

.input-bar {
  display: flex;
  gap: 10px;
  padding: 14px 20px;
  border-top: 1px solid var(--border);
  background: var(--bg);
}

.input-bar input {
  flex: 1;
  background: var(--surface);
  border: 1px solid var(--border);
  color: var(--text);
  padding: 10px 16px;
  border-radius: var(--radius);
  font-size: 15px;
  outline: none;
  transition: border-color 0.15s;
}

.input-bar input:focus { border-color: var(--accent); }

.input-bar button {
  background: var(--accent);
  border: none;
  color: #fff;
  padding: 10px 20px;
  border-radius: var(--radius);
  font-size: 15px;
  cursor: pointer;
  transition: opacity 0.15s;
}

.input-bar button:hover { opacity: 0.85; }
.input-bar button:disabled { opacity: 0.4; cursor: not-allowed; }

.voice-view {
  position: fixed;
  top: 53px; bottom: 0; left: 0; right: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 28px;
}

.voice-name {
  font-size: 48px;
  font-weight: 300;
  letter-spacing: 0.15em;
  color: var(--text);
}

.voice-status {
  font-size: 14px;
  color: var(--text-muted);
  letter-spacing: 0.08em;
  min-height: 20px;
  text-align: center;
}

.push-to-talk {
  position: relative;
  width: 80px;
  height: 80px;
  border-radius: 50%;
  background: var(--accent);
  border: none;
  cursor: pointer;
  transition: transform 0.1s;
  display: flex;
  align-items: center;
  justify-content: center;
}

.push-to-talk:active,
.push-to-talk.recording { transform: scale(0.93); }

.ptt-ring {
  position: absolute;
  inset: -6px;
  border-radius: 50%;
  border: 2px solid transparent;
  transition: border-color 0.2s;
}

.push-to-talk.recording .ptt-ring {
  border-color: var(--accent);
  animation: pulse-ring 1.2s ease-in-out infinite;
}

@keyframes pulse-ring {
  0%, 100% { opacity: 0.5; transform: scale(1); }
  50%       { opacity: 1;   transform: scale(1.08); }
}

.voice-hint { font-size: 12px; color: var(--text-muted); }

.voice-transcript {
  max-width: 340px;
  text-align: center;
  font-size: 14px;
  color: var(--text-muted);
  min-height: 40px;
  padding: 0 20px;
}

.thinking { display: inline-flex; gap: 4px; padding: 4px 0; }

.thinking span {
  width: 5px; height: 5px;
  border-radius: 50%;
  background: var(--text-muted);
  animation: blink 1.2s ease-in-out infinite;
}

.thinking span:nth-child(2) { animation-delay: 0.2s; }
.thinking span:nth-child(3) { animation-delay: 0.4s; }

@keyframes blink {
  0%, 80%, 100% { opacity: 0.2; }
  40%           { opacity: 1; }
}

.memory-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.6);
  display: flex;
  align-items: flex-end;
  justify-content: flex-end;
  z-index: 100;
}

.memory-panel {
  width: 360px;
  max-width: 100%;
  height: 100%;
  background: var(--surface);
  border-left: 1px solid var(--border);
  display: flex;
  flex-direction: column;
}

.memory-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 20px;
  border-bottom: 1px solid var(--border);
  font-size: 14px;
  font-weight: 500;
}

.memory-header button {
  background: none;
  border: none;
  color: var(--text-muted);
  font-size: 22px;
  cursor: pointer;
  line-height: 1;
}

.memory-list {
  flex: 1;
  overflow-y: auto;
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.memory-item {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  padding: 12px 14px;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  gap: 12px;
}

.memory-item-body { flex: 1; min-width: 0; }

.memory-item-key {
  font-size: 11px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin-bottom: 3px;
}

.memory-item-value {
  font-size: 14px;
  color: var(--text);
  word-break: break-word;
}

.memory-item-conf {
  font-size: 11px;
  color: var(--text-muted);
  margin-top: 3px;
}

.memory-item button {
  background: none;
  border: none;
  color: var(--text-muted);
  cursor: pointer;
  font-size: 18px;
  flex-shrink: 0;
  padding: 0 2px;
  transition: color 0.15s;
}

.memory-item button:hover { color: var(--danger); }

.empty-state {
  color: var(--text-muted);
  font-size: 14px;
  text-align: center;
  padding: 40px 0;
}

.hidden { display: none !important; }
```

- [ ] **Step 2: Commitear**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps"
git add venvis/client/style.css
git commit -m "feat(venvis): dark minimalist CSS"
```

---

## Task 8: Frontend App Logic (`client/app.js`)

**Files:**
- Create: `venvis/client/app.js`

La diferencia clave respecto a William: el STT usa `SpeechRecognition` del browser en lugar de `MediaRecorder` + envío de audio al servidor.

- [ ] **Step 1: Crear app.js**

Crear `venvis/client/app.js`:

```javascript
/* global io, marked */

const socket = io()
const SESSION_ID = 'agus'

let currentMode = 'chat'
let isRecording = false
let recognition = null
let pendingVenvisBubble = null

const chatView       = document.getElementById('chatView')
const voiceView      = document.getElementById('voiceView')
const messages       = document.getElementById('messages')
const chatInput      = document.getElementById('chatInput')
const btnSend        = document.getElementById('btnSend')
const btnMode        = document.getElementById('btnMode')
const modeLabel      = document.getElementById('modeLabel')
const btnPTT         = document.getElementById('btnPTT')
const voiceStatus    = document.getElementById('voiceStatus')
const voiceTrans     = document.getElementById('voiceTranscript')
const btnMemory      = document.getElementById('btnMemory')
const memoryOverlay  = document.getElementById('memoryOverlay')
const memoryList     = document.getElementById('memoryList')
const btnCloseMemory = document.getElementById('btnCloseMemory')

// ── SPEECH RECOGNITION SETUP ──
const SpeechRecognitionAPI = window.SpeechRecognition || window.webkitSpeechRecognition

function initSpeechRecognition() {
  if (!SpeechRecognitionAPI) {
    voiceStatus.textContent = 'Solo disponible en Chrome/Edge'
    btnPTT.disabled = true
    return
  }
  recognition = new SpeechRecognitionAPI()
  recognition.lang = 'es-AR'
  recognition.continuous = false
  recognition.interimResults = false

  recognition.onstart = () => {
    isRecording = true
    btnPTT.classList.add('recording')
    voiceStatus.textContent = 'Escuchando...'
    voiceTrans.textContent = ''
  }

  recognition.onresult = (e) => {
    const text = e.results[0][0].transcript.trim()
    if (!text) return
    voiceTrans.textContent = `Vos: ${text}`
    voiceStatus.textContent = 'Pensando...'
    socket.emit('user_message', { text, sessionId: SESSION_ID })
  }

  recognition.onerror = (e) => {
    console.error('[STT]', e.error)
    voiceStatus.textContent = e.error === 'not-allowed'
      ? 'Permiso de micrófono denegado'
      : 'Listo'
    isRecording = false
    btnPTT.classList.remove('recording')
  }

  recognition.onend = () => {
    isRecording = false
    btnPTT.classList.remove('recording')
    if (voiceStatus.textContent === 'Escuchando...') {
      voiceStatus.textContent = 'Listo'
    }
  }
}

initSpeechRecognition()

// ── MODO TOGGLE ──
btnMode.addEventListener('click', () => {
  currentMode = currentMode === 'chat' ? 'voice' : 'chat'
  if (currentMode === 'chat') {
    chatView.classList.remove('hidden')
    voiceView.classList.add('hidden')
    modeLabel.textContent = 'Voz'
  } else {
    chatView.classList.add('hidden')
    voiceView.classList.remove('hidden')
    modeLabel.textContent = 'Chat'
    voiceStatus.textContent = 'Listo'
  }
})

// ── CHAT ──
function sendChatMessage() {
  const text = chatInput.value.trim()
  if (!text) return
  chatInput.value = ''
  appendUserMessage(text)
  btnSend.disabled = true
  chatInput.disabled = true
  pendingVenvisBubble = appendVenvisThinking()
  socket.emit('user_message', { text, sessionId: SESSION_ID })
}

btnSend.addEventListener('click', sendChatMessage)
chatInput.addEventListener('keydown', (e) => {
  if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); sendChatMessage() }
})

function appendUserMessage(text) {
  const div = document.createElement('div')
  div.className = 'msg-user'
  div.textContent = text
  messages.appendChild(div)
  scrollBottom()
}

function appendVenvisThinking() {
  const div = document.createElement('div')
  div.className = 'msg-venvis'
  div.innerHTML = '<div class="thinking"><span></span><span></span><span></span></div>'
  messages.appendChild(div)
  scrollBottom()
  return div
}

function scrollBottom() {
  messages.scrollTop = messages.scrollHeight
}

// ── SOCKET EVENTS ──
socket.on('venvis_chunk', ({ text }) => {
  if (!pendingVenvisBubble) {
    pendingVenvisBubble = document.createElement('div')
    pendingVenvisBubble.className = 'msg-venvis'
    messages.appendChild(pendingVenvisBubble)
  }
  const thinking = pendingVenvisBubble.querySelector('.thinking')
  if (thinking) thinking.remove()
  pendingVenvisBubble.dataset.raw = (pendingVenvisBubble.dataset.raw || '') + text
  pendingVenvisBubble.innerHTML = marked.parse(pendingVenvisBubble.dataset.raw)
  scrollBottom()
})

socket.on('venvis_done', ({ text }) => {
  if (pendingVenvisBubble) {
    pendingVenvisBubble.innerHTML = marked.parse(text)
    pendingVenvisBubble.dataset.raw = ''
    pendingVenvisBubble = null
  }
  btnSend.disabled = false
  chatInput.disabled = false
  chatInput.focus()
  scrollBottom()
  if (currentMode === 'voice') voiceStatus.textContent = 'Listo'
})

socket.on('venvis_audio', ({ audioBase64 }) => {
  const bytes = Uint8Array.from(atob(audioBase64), c => c.charCodeAt(0))
  const blob = new Blob([bytes], { type: 'audio/mpeg' })
  const url = URL.createObjectURL(blob)
  const audio = new Audio(url)
  if (currentMode === 'voice') voiceStatus.textContent = 'Hablando...'
  audio.addEventListener('ended', () => {
    URL.revokeObjectURL(url)
    if (currentMode === 'voice') voiceStatus.textContent = 'Listo'
  })
  audio.play().catch(() => {})
})

socket.on('venvis_error', ({ message }) => {
  pendingVenvisBubble = null
  btnSend.disabled = false
  chatInput.disabled = false
  const div = document.createElement('div')
  div.className = 'msg-error'
  div.textContent = message
  messages.appendChild(div)
  scrollBottom()
  if (currentMode === 'voice') voiceStatus.textContent = 'Listo'
})

// ── VOZ: push-to-talk con SpeechRecognition ──
function startListening() {
  if (!recognition || isRecording) return
  try {
    recognition.start()
  } catch (err) {
    console.error('[PTT] start error:', err)
  }
}

function stopListening() {
  if (!recognition || !isRecording) return
  recognition.stop()
}

btnPTT.addEventListener('mousedown', startListening)
btnPTT.addEventListener('touchstart', (e) => { e.preventDefault(); startListening() }, { passive: false })
btnPTT.addEventListener('mouseup', stopListening)
btnPTT.addEventListener('touchend', (e) => { e.preventDefault(); stopListening() }, { passive: false })
btnPTT.addEventListener('mouseleave', stopListening)

// ── MEMORIA ──
btnMemory.addEventListener('click', async () => {
  memoryOverlay.classList.remove('hidden')
  await loadMemory()
})

btnCloseMemory.addEventListener('click', () => {
  memoryOverlay.classList.add('hidden')
})

memoryOverlay.addEventListener('click', (e) => {
  if (e.target === memoryOverlay) memoryOverlay.classList.add('hidden')
})

async function loadMemory() {
  memoryList.innerHTML = '<p class="empty-state">Cargando...</p>'
  const res = await fetch(`/api/memory?session=${SESSION_ID}`)
  const items = await res.json()
  if (!items.length) {
    memoryList.innerHTML = '<p class="empty-state">Sin recuerdos aún.</p>'
    return
  }
  memoryList.innerHTML = ''
  for (const item of items) {
    const div = document.createElement('div')
    div.className = 'memory-item'
    div.innerHTML = `
      <div class="memory-item-body">
        <div class="memory-item-key">${escapeHtml(item.key)}</div>
        <div class="memory-item-value">${escapeHtml(item.value)}</div>
        <div class="memory-item-conf">Confianza: ${item.confidence}/10</div>
      </div>
      <button data-key="${escapeHtml(item.key)}" title="Olvidar">×</button>
    `
    div.querySelector('button').addEventListener('click', async (e) => {
      const key = e.currentTarget.dataset.key
      await fetch(`/api/memory/${encodeURIComponent(key)}?session=${SESSION_ID}`, { method: 'DELETE' })
      await loadMemory()
    })
    memoryList.appendChild(div)
  }
}

function escapeHtml(str) {
  return str
    .replace(/&/g, '&amp;').replace(/</g, '&lt;')
    .replace(/>/g, '&gt;').replace(/"/g, '&quot;')
}
```

- [ ] **Step 2: Commitear**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps"
git add venvis/client/app.js
git commit -m "feat(venvis): frontend con SpeechRecognition STT"
```

---

## Task 9: PWA Assets

**Files:**
- Create: `venvis/client/manifest.json`
- Create: `venvis/client/icon.svg`
- Create: `venvis/client/sw.js`

- [ ] **Step 1: Crear manifest.json**

Crear `venvis/client/manifest.json`:

```json
{
  "name": "VENVIS",
  "short_name": "VENVIS",
  "description": "Asistente personal con IA",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#0a0a0a",
  "theme_color": "#0a0a0a",
  "icons": [
    { "src": "icon.svg", "sizes": "any", "type": "image/svg+xml" }
  ]
}
```

- [ ] **Step 2: Crear icon.svg**

Crear `venvis/client/icon.svg`:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <rect width="512" height="512" fill="#0a0a0a"/>
  <text
    x="256" y="340"
    text-anchor="middle"
    font-family="system-ui, -apple-system, sans-serif"
    font-size="260"
    font-weight="300"
    fill="#e8e8e8"
  >V</text>
</svg>
```

- [ ] **Step 3: Crear sw.js**

Crear `venvis/client/sw.js`:

```javascript
const CACHE = 'venvis-v1'
const ASSETS = ['/', '/index.html', '/style.css', '/app.js', '/manifest.json', '/icon.svg']

self.addEventListener('install', (e) => {
  e.waitUntil(caches.open(CACHE).then(c => c.addAll(ASSETS)))
  self.skipWaiting()
})

self.addEventListener('activate', (e) => {
  e.waitUntil(
    caches.keys().then(keys =>
      Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k)))
    )
  )
  self.clients.claim()
})

self.addEventListener('fetch', (e) => {
  if (e.request.method !== 'GET') return
  if (e.request.url.includes('/api/') || e.request.url.includes('socket.io')) return
  e.respondWith(caches.match(e.request).then(cached => cached || fetch(e.request)))
})
```

- [ ] **Step 4: Commitear**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps"
git add venvis/client/manifest.json venvis/client/icon.svg venvis/client/sw.js
git commit -m "feat(venvis): PWA manifest, icon y service worker"
```

---

## Task 10: Config Nginx y Script de Deploy

**Files:**
- Create: `venvis/nginx.conf`
- Create: `venvis/deploy.sh`

- [ ] **Step 1: Crear nginx.conf**

Crear `venvis/nginx.conf`:

```nginx
server {
    listen 443 ssl;
    server_name venvis.orvexautomation.com;

    ssl_certificate     /etc/letsencrypt/live/venvis.orvexautomation.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/venvis.orvexautomation.com/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }
}

server {
    listen 80;
    server_name venvis.orvexautomation.com;
    return 301 https://$host$request_uri;
}
```

- [ ] **Step 2: Crear deploy.sh**

Crear `venvis/deploy.sh`:

```bash
#!/bin/bash
# Deploy VENVIS al VPS
# Uso: bash deploy.sh
# Prerequisito: SSH key configurada para root@31.97.21.155

VPS="root@31.97.21.155"
REMOTE_DIR="/var/www/venvis"
LOCAL_DIR="$(dirname "$0")"

echo "→ Sincronizando archivos..."
rsync -avz --exclude='node_modules' --exclude='.env' --exclude='venvis.db' \
  "$LOCAL_DIR/" "$VPS:$REMOTE_DIR/"

echo "→ Instalando dependencias en el VPS..."
ssh "$VPS" "cd $REMOTE_DIR && npm install --omit=dev"

echo "→ Reiniciando VENVIS con PM2..."
ssh "$VPS" "cd $REMOTE_DIR && pm2 restart venvis 2>/dev/null || pm2 start server/index.mjs --name venvis"

echo "→ Verificando..."
sleep 2
ssh "$VPS" "pm2 show venvis | grep -E '(status|restarts)'"

echo "✓ Deploy completo — https://venvis.orvexautomation.com"
```

- [ ] **Step 3: Commitear**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps"
git add venvis/nginx.conf venvis/deploy.sh
git commit -m "feat(venvis): Nginx config y script de deploy"
```

---

## Task 11: Setup Inicial en el VPS

**Prerequisito:** Acceso SSH a `root@31.97.21.155`. Ejecutar estos comandos UNO POR UNO desde una terminal local.

- [ ] **Step 1: Conectarse al VPS y verificar Node.js**

```bash
ssh root@31.97.21.155
node --version
```

Expected: `v22.x.x` o superior. Si Node.js no está instalado:
```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
apt-get install -y nodejs
```

- [ ] **Step 2: Instalar dependencias del sistema**

```bash
# En el VPS:
apt-get update
apt-get install -y nginx python3 python3-pip certbot python3-certbot-nginx
pip3 install edge-tts
npm install -g pm2
```

Verificar edge-tts:
```bash
edge-tts --voice "es-AR-TomasNeural" --text "VENVIS online" --write-media /tmp/test.mp3 && echo "✓ edge-tts OK" && rm /tmp/test.mp3
```

- [ ] **Step 3: Crear directorio y copiar código**

```bash
# En el VPS:
mkdir -p /var/www/venvis
exit
```

```bash
# En local (desde la carpeta CreadorApps):
bash venvis/deploy.sh
```

Expected: `✓ Deploy completo — https://venvis.orvexautomation.com`

- [ ] **Step 4: Crear .env en el VPS**

```bash
ssh root@31.97.21.155
cat > /var/www/venvis/.env << 'EOF'
ANTHROPIC_API_KEY=sk-ant-REDACTED
PORT=3000
SESSION_NAME=agus
EOF
```

- [ ] **Step 5: Arrancar VENVIS con PM2**

```bash
# En el VPS:
cd /var/www/venvis
pm2 start server/index.mjs --name venvis
pm2 startup
pm2 save
```

Verificar:
```bash
curl -s http://localhost:3000/api/memory
```

Expected: `[]`

- [ ] **Step 6: Configurar Nginx**

```bash
# En el VPS:
cp /var/www/venvis/nginx.conf /etc/nginx/sites-available/venvis
ln -sf /etc/nginx/sites-available/venvis /etc/nginx/sites-enabled/venvis
nginx -t
```

Expected: `nginx: configuration file /etc/nginx/nginx.conf test is successful`

Si hay un default site que ocupe el puerto 80:
```bash
rm -f /etc/nginx/sites-enabled/default
nginx -t && systemctl reload nginx
```

- [ ] **Step 7: Obtener certificado SSL**

Prerequisito: el DNS `venvis.orvexautomation.com` debe apuntar a `31.97.21.155` antes de este paso.

```bash
# En el VPS:
certbot --nginx -d venvis.orvexautomation.com --non-interactive --agree-tos -m orvex.aiii@gmail.com
```

Expected: `Congratulations! Your certificate and chain have been saved...`

- [ ] **Step 8: Reload Nginx con SSL activo**

```bash
# En el VPS:
systemctl reload nginx
```

- [ ] **Step 9: Verificar HTTPS**

```bash
# En local:
curl -s -o /dev/null -w "HTTP %{http_code}\n" https://venvis.orvexautomation.com/api/memory
```

Expected: `HTTP 200`

- [ ] **Step 10: Abrir en Chrome y probar**

1. Abrir `https://venvis.orvexautomation.com` en Chrome
2. Escribir un mensaje: "Hola VENVIS, me llamo Charly"
3. Verificar: texto aparece en streaming, audio reproduce automáticamente
4. Cambiar a modo Voz → mantener presionado el botón → hablar → soltar
5. Verificar: el browser pide permiso de micrófono la primera vez
6. Buscar ícono de instalación en la barra de Chrome (⊕) → instalar PWA

- [ ] **Step 11: Commit final**

```bash
cd "c:/Users/Charly/OneDrive/Escritorio/ClaudeCode/CreadorApps"
git add venvis/
git commit -m "feat(venvis): VENVIS completo — PWA con voz, memoria y deploy VPS"
```

---

## Post-Deploy

Para actualizar después de cambios en el código:
```bash
bash venvis/deploy.sh
```

Para ver logs en tiempo real:
```bash
ssh root@31.97.21.155 "pm2 logs venvis"
```

Para instalar en Android:
1. Abrir `https://venvis.orvexautomation.com` en Chrome
2. Menú → "Agregar a pantalla de inicio"
3. VENVIS aparece como app nativa con ícono "V"
